name: GovBot Collector

on:
  schedule:
    - cron: '0 0 * * 1-5'  # 평일만 UTC 0시 = KST 오전 9시 (월~금)
  workflow_dispatch:       # 수동 실행 가능

permissions:
  contents: write

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Run collector
        env:
          BIZINFO_API_KEY: ${{ secrets.BIZINFO_API_KEY }}
          GOV24_API_KEY: ${{ secrets.GOV24_API_KEY }}
          FORCE_RUN: ${{ github.event_name == 'workflow_dispatch' && 'true' || 'false' }}
        run: python scripts/collect.py

      - name: Commit & push updated data
        run: |
          git config user.name 'govbot'
          git config user.email 'govbot@github.com'
          git add site/data/announcements.json seen_ids.json
          git diff --cached --quiet || (git commit -m "Update announcements $(date +%Y-%m-%d)" && git push)
